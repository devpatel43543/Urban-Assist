apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  storageClassName: "mysql-storage-class"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 
